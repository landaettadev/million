namespace RealEstate.Application.Endpoints;

// Marker class to keep consistency with requested structure.
// The actual HTTP endpoint mappings live in the API layer to avoid ASP.NET Core dependencies here.
public static class PropertyEndpointsMarker { }
